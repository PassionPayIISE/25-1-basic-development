let person = {
    name : Alice,
    age : 25,
    isEmployed : false,
    address : undefined,
    pet : null,
}

let mixed = [1, 2, 3, "four", false]; 

let 2cool = true; // 사용불가, 숫자로 시작할 수 없음
let private = "secret"; // 사용불가, 키워드로 변수를 지정할 수 없음
let $doller = 1000; // 사용가능
let my-name ="lee"; // 사용 불가, -은 연산자로 인식됨
let camelCaseName = "OK"; //사용가능능
